apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: mern
spec:
  type: NodePort
  ports:
  - port: 3500
    targetPort: 3500
    protocol: TCP
    nodePort: 32000  # Optional: specify a port within the range 30000-32767
  selector:
    role: api

